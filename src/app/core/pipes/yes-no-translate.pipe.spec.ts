import { YesNoTranslatePipe } from './yes-no-translate.pipe';

describe('YesNoTranslatePipe', () => {
  it('create an instance', () => {
    const pipe = new YesNoTranslatePipe();
    expect(pipe).toBeTruthy();
  });
});
